# Copy to .env only if the default host ports clash with something you
# already run locally. Containers always talk to each other on the
# defaults; these only change what's exposed on your machine.
#POSTGRES_PORT=5432
#REDIS_PORT=6379
#MINIO_PORT=9000
#MINIO_CONSOLE_PORT=9001
